## Usage
Requires a `.env` file with the following keys:
```
# Requred if outbound UDP is blocked 
TUNNEL_TRANSPORT_PROTOCOL=http2

# Token generated from Cloudflare Tunnel config
TUNNEL_TOKEN=[some_cf_token]
```